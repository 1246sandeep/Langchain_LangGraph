[project]
name = "langchain-course-main"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sandeep Tingilikari", email = "1246.sandeept@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "black>=26.5.1",
    "isort>=8.0.1",
    "langchain>=1.3.15",
    "langchain-ollama>=1.1.0",
    "langchain-openai>=1.5.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
langchain-course-main = "langchain_course_main:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
